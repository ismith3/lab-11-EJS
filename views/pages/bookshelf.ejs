<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>Document</title>
</head>
<body>
  <header>
    <% include ../partials/nav %>
    <section>Search Results</section>
  </header>
  <% data.forEach(book => { %>
    <section class="book">
      <% if(book.thumbnail === 'Image not found') { %>
        <h3 class="noImage">Image not found</h3>
      <% } else { %>
        <img src="<%= book.thumbnail.thumbnail.replace('http', 'https'); %>">
      <% } %>
      <h2><%= book.title; %></h2>
      <h3><%= book.authors; %></h3>
      <p><%= book.description; %></p>
    </section>
  <% }); %>
</body>
</html>